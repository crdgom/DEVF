/* Crea una función llamada square() que reciba como parámetro un arreglo
de números positivos y que retorne los elementos elevados al cuadrado
NOTA: puedes utilizar map() */

function square(arr) {
   return arr.map((num) => num * num);
}